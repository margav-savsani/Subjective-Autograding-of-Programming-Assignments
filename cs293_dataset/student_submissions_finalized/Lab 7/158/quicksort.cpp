#ifndef STD_HEADERS
#include "std_headers.h"
#endif

#ifndef PLANNER_H
#include "planner.h"
#endif

#ifndef DICTIONARY_H
#include "dictionary.h"
#endif

#ifndef CODES_H
#include "codes.h"
#endif

using namespace std;

listOfObjects<TrainInfoPerStation *> * compare


void Partition(listOfObjects<TrainInfoPerStation *> *leftptr,listOfObjects<TrainInfoPerStation *> *pivotptr, listOfObjects<TrainInfoPerStation *> *rightptr){
    listOfObjects<TrainInfoPerStation *> *leftmover=leftptr;
    listOfObjects<TrainInfoPerStation *> *rightmover=rightptr;


    while(leftptr->next!=pivotptr && leftptr->object->){


    }
}




void Quicksort(listOfObjects<TrainInfoPerStation *> *stnInfoList)
{
  { //Input of space-time trade-off variable
  int K; //trade-off variable
  cout<<"Enter value for space-time trade-off variable K: ";
  cin>>K;
  cout<<endl;
  }

  {
    /**
     * @brief TODO
     * 
     */
  
      listOfObjects<TrainInfoPerStation*> *pivot_pointer;
  }

  {
      Quicksort()



  }

}
