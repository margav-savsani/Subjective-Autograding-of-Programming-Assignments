#ifndef STD_HEADERS
#include "std_headers.h"
#endif

#ifndef PLANNER_H
#include "planner.h"
#endif

#ifndef DICTIONARY_H
#include "dictionary.h"
#endif

#ifndef CODES_H
#include "codes.h"
#endif

void Quicksort(listOfObjects<TrainInfoPerStation *> *stnInfoList)
{
  // Put your code here.
  listOfObjects <TrainInfoPerStation* >* currInfoList = stnInfoList;
  
  int k = 4;
  int count = 0;
  while(currInfoList != nullptr){
    if (count % k == 0){

    }
    count++;
  
  }
  listOfObjects<TrainInfoPerStation*>* pivot;

  listOfObjects <TrainInfoPerStation* >* leftPtr = stnInfoList;
  listOfObjects <TrainInfoPerStation* >* rightPtr = pivot->next;  
  while(leftPtr != nullptr)



}
