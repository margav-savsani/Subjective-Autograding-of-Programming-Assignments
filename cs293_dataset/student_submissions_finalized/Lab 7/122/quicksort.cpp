#ifndef STD_HEADERS
#include "std_headers.h"
#endif

#ifndef PLANNER_H
#include "planner.h"
#endif

#ifndef DICTIONARY_H
#include "dictionary.h"
#endif

#ifndef CODES_H
#include "codes.h"
#endif
listOfObjects<TrainInfoPerStation *> * random_pivot(listOfObjects<TrainInfoPerStation *> *stnInfoList){
  
}
void Quicksort(listOfObjects<TrainInfoPerStation *> *stnInfoList)
{
  for (int i = 0; i < DICT_SIZE; i++) {

      // Lab 7 specific
      stationInfo[i] = nullptr;
      // End lab 7 specific

      for (int j = 0; j < DICT_SIZE; j++) {
	jCRMatrix[i][j] = nullptr;
      }
    }
    stnNamesTrie = new Trie(&stnNameToIndex);
  
}
